# AI Company 순서형 미리보기

압축을 풀고 `AI-Company-journey.html`을 Chrome에서 엽니다. GitHub 로그인·운영 로그인 없이 여는 읽기용 제품 미리보기입니다. 파일 앱의 간이 미리보기에서는 JavaScript가 제한될 수 있습니다.

1. 프로젝트를 선택하고 `계획 → 진행 → 승인 → 결과`를 오갑니다.
2. `진행`에서 최근/이전 실행, 역할·전달선·자기 이관·반환을 선택합니다. `대상 정보`와 같은 실행의 결과·승인을 대조합니다.
3. Light/Black, 전체 보기·이동·확대, 상세 닫기, 뒤로가기·새로고침을 확인합니다.

공개 합성 자료이며 운영 기록이 아닙니다. 실제 제품 JS/CSS를 묶었으나 GET 응답은 고정 자료입니다. 목표·메시지 전송, 명세 저장, 계획 확정, 후보 결정, 외부 링크·모델·서비스워커는 실행하지 않습니다. 실제 쓰기·상태·서비스워커 검증은 별도의 임시 API·SQLite·격리 Chrome에서 수행했습니다. 만료 시각을 되돌리지 않으므로 시험 승인도 시간이 지나면 만료로 표시됩니다.

제품: `14114a71584c65c6dbbd0b33a1a26c7246d1e1ad`

검사: `14114a71584c65c6dbbd0b33a1a26c7246d1e1ad`

검증 실행: https://github.com/HyungwonPark/ai-company/actions/runs/35933619143

HTML·manifest는 이 CI에서 실제 검사한 파일 그대로입니다. manifest의 `source_commit`은 PR 합성 checkout `82c794cd2d511950377a3caef71670e55877dc56`이며 head와 다릅니다. 원본 28개 파일 SHA-256을 제품 커밋과 대조해 모두 일치했습니다. 다른 CI의 동명 HTML로 교체하지 않았습니다.

R25 보완: 완료·진행·대기·확인 필요·미확인을 구별하고, 반려와 저장 원문을 보존합니다. 실행 선택기의 일반 대기를 재개 예약으로 단정하지 않습니다. 신규 부팅 모듈 의존성을 제거한 서비스워커 호환 수정도 제품에 포함되지만 이 오프라인 HTML에서 그 생명주기를 재현하지는 않습니다.

`SHA256SUMS`로 확인하세요. Linux: `sha256sum -c SHA256SUMS`. macOS: `shasum -a 256 -c SHA256SUMS`. Windows PowerShell: `Get-FileHash .\AI-Company-journey.html -Algorithm SHA256`.

배포·병합·APK 직접 조작·실제 모델 전체 실행 완료를 뜻하지 않습니다.
