# 새 작업실 공개 적용 · 2026-09-16

사용자가 `f42040e`의 [고정 적용안](pr12-ux-validation-2026-09-16.md)을 보고 **“후속 웹 후보 적용 승인”**이라고 답한 뒤, 웹 후보 **`28404b2885a215ccff15df23d36550b7c67c83bb`**를 https://hyungwon.cloud 에 적용했다. 완료 시각은 2026-09-16 **16:33:42 KST**다. [실제 적용 증거](evidence/pr12-web-activation-2026-09-16.json)

## 실제 적용 완료

- 웹 이미지: `sha256:c10c4b99128627552b4a24f2289a44c99d849de1305be3975a539511cc04b33f`. 기존 Compose의 **image 한 필드**만 교체했다. 정상 종료·새 컨테이너 시작·healthy 후 HTTPS 27개 파일의 SHA-256을 대조했다.
- 원래 DB의 **27개 테이블 모든 행은 동일**했다. `management_project_creations`만 추가했다. 계정·PM 요청·실행·계획·원문·번역·사용량·승인 기록을 재작성하거나 과거 DB로 복원하지 않았다.
- 두 worker는 `control-plane-5594744`를 유지했다. automation PID `2336705`, translation PID `2336706`, 재시작 수 모두 0으로 전후 같았다. 설정·전용 환경변수·서비스 파일도 고정 기준과 일치했다. Claude 개발 대체 후보는 기존 승인대로 활성화 상태다.
- 커플 앱·Caddy·DB·백업 컨테이너의 ID·PID·시작 시각·재시작 수가 같았다. quota timer active, assetlinks 동일, 커플 HTTPS 200을 확인했다. PR #10 후보 승인은 **pending**이다.
- 비밀번호 로그인 모드, 익명 API 401, 잘못된 Host/Origin 403을 확인했다. API 포트를 공개하지 않았으며 격리 조건을 유지했다. 실제 사용자 비밀번호 로그인은 대행하지 않았고 인증된 세션·CSRF·쿠키는 [원격 후보 회귀](https://github.com/HyungwonPark/ai-company/actions/runs/35068287030)의 검사와 구분한다.
- 이 웹 교체에서 모델 호출·사용자 프로젝트 생성·계획 확정·병합은 **0건**이다. 서명된 기존 APK 0.1.1과 도메인 인증서 연결을 그대로 사용하며, 새 APK를 배포했다고 표시하지 않는다.

앞서 승인한 `5594744` worker/Claude 적용과 번역 복구는 [별도 운영 기록](production-activation-2026-09-16.md)에 보존했다. 이전 `c2b3fec` 교체를 중복 수행하지 않았다.

## 구현·검증 완료지만 미적용

이번 고정 UI 후보의 공개 미적용 항목은 없다. [UI 검수·원격 CI·모바일 화면·DB 호환성](pr12-ux-validation-2026-09-16.md)은 같은 실행 후보에 연결한다. 저장소의 이후 문서 커밋을 별도 실행 버전으로 교체하지 않는다.

암호화된 서명키 백업 파일과 외부 복원 절차는 준비돼 있지만, 실제 외부 보관/복원은 아래 마스터 확인 항목이다. 일반 프로젝트 자유 실행은 구현 완료 항목이 아니다.

## 마스터 확인 필요

1. **APK를 완전히 닫고 다시 열거나 https://hyungwon.cloud 에 접속**한다. `edward`와 본인이 변경한 비밀번호로 로그인한다. 기존 로그인·비밀번호를 초기화하지 않았다.
2. 첫 화면은 **프로젝트**다. 예전 링크로 매니저가 열렸다면 상단 **전체**를 누른다. **새 프로젝트 +**를 누른다.
3. 새 이름 예시: **역할 상태 확인**. 이 이름의 프로젝트를 서버가 미리 생성하지 않았다.
4. 목표 예시: **“작업 상태별 개수를 세는 작은 기능을 만들고, 개발과 검사를 두 역할로 나눠 빈 입력과 알 수 없는 상태까지 확인해 주세요.”**
5. **PM과 시작**을 누른다. 기대 결과는 새 프로젝트와 첫 PM 요청 각각 1건 저장, 해당 프로젝트의 매니저 이동이다. 아직 개발 작업이나 확정 실행은 없다. 응답이 끊기면 같은 입력으로 재시도한다.
6. 실제 PM 답변을 읽고 필요한 대화를 진행한다. 역할·담당·완료 조건이 맞으면 **계획 검토·확정**을 누른다. 원문과 읽기용 한국어, 범위·예산을 확인하고 확인 항목을 체크한 뒤 **이 계획 확정**을 직접 누른다.
7. 기대 결과는 해당 목표 revision·계획 hash에 연결된 **실행 1건**이다. 진행 화면에서 실제 작업과 기다리는 역할을 확인한다. 서버 Codex는 같은 실행 ID의 병렬 개발·검사·원격 CI·독립 검수·Astra Ultra 최종 검수·종합 보고·새 후보 승인 요청을 대조한다. 이 단계는 아직 사용자 조작 전이다.

**현재 자동 실행은 `src/ai_company/pilot_status.py`, `tests/test_pilot_status.py`, 기존 CI 요청 메타데이터로 제한된다.** 위 자연어 목표는 이 범위를 위한 작은 검증이다. 서버 구축 전체 목표나 일반 프로젝트를 자동으로 수정하도록 권한을 넓히지 않았다. 계획 확정은 PR #10 후보 수용이나 배포·병합 승인이 아니다.

서명키의 **PC 비공개 폴더 보관, 별도 비밀번호 관리자에 복구 코드 보관, 외부 복원 확인**도 마스터가 수행한다. [준비한 인수인계 절차](live-use-readiness-2026-09-16.md#서명키인수인계)를 사용한다. 같은 서버의 암호화 사본·로컬 복원 검사를 외부 백업 완료로 세지 않는다. 실제 호스트 재부팅 관측도 수행하지 않았다.

## 문제로 차단

원래 번역 실패 6건은 보존했다. 승인된 복구에서 4건은 표시 자료로 반영했고, 2건은 보호 검사에서 ‘아닙니다’/‘미결’ 표현을 인식하지 못해 실패 상태다. 승인된 추가 호출 4회를 사용했으며 추가 호출·재예약은 없다. 이 두 문서는 원문으로 확인한다. 후속 파서 버전·반례·저장 결과 재검사안을 검토하기 전 기존 실패를 PASS로 바꾸지 않는다. 다른 역할과 보고·승인 기능은 이 번역 실패를 기다리지 않는다.

## 복구

복구 이미지 `sha256:399e353b856c71b234c2bd984d9092cfddb789dd1ea6077c4ae84c38da9cc55e`와 원본 Compose를 보존한다. 서버 비공개 `.ai-company/pr12-console-20260916/`에 원본/제안 Compose, 실제 승인과 적용 단계, 변경 전 DB 감사 사본, 검증 결과가 있다.

복구가 필요하면 현재 Compose가 이번 제안과 같은지 확인하고 **웹 컨테이너만** 정상 종료한 뒤 원본 이미지·Compose로 되돌린다. 두 worker·Caddy·타이머를 재시작하지 않는다. DB 감사 사본은 복구에 쓰지 않으며 새 생성 식별자·프로젝트·사용량·승인 기록을 그대로 남긴다. 오래된 브라우저 요청은 화면을 새로 연 뒤 원장의 생성 결과부터 대조한다.
